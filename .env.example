# Where the game server lives, baked into the client at build time.
# Only needed when the client and server are hosted separately; leave unset
# locally, where Vite proxies /ws to the server on localhost:8787.
VITE_WS_URL=https://deep-sea-server.onrender.com
